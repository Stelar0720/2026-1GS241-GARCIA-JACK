{
  "name": "sinnoh-edition-pwc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api": "cd apps/api && bun run dev",
    "web": "cd apps/web && bun run dev",
    "build": "cd apps/web && bun run build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
